<Window x:Class="WpfPlannerApp.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Вход"
        Width="420"
        Height="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Background="{StaticResource BgBrush}">
        <Border Width="320"
                Padding="22"
                Background="White"
                CornerRadius="16"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">

            <StackPanel>

                <TextBlock Text="Добро пожаловать!"
                           FontSize="26"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource PrimaryBrush}"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,18"/>

                <TextBlock Text="Логин" FontSize="13"/>
                <TextBox x:Name="LoginBox" Height="36" Margin="0,0,0,12"/>

                <TextBlock Text="Пароль" FontSize="13"/>
                <PasswordBox x:Name="PasswordBox" Height="36" Margin="0,0,0,20"/>

                <Button Content="Войти"
                        Height="38"
                        Background="{StaticResource AccentBrush}"
                        Click="Login_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
